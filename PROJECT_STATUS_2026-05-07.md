# Статус проекта и хронология изменений

## Обновление на 2026-05-13

### Кратко

Сегодня работа была сосредоточена уже не на крупных новых фичах, а на аккуратной доводке проекта после аудита:

1. закрыты несколько практических runtime/security/type-safety хвостов;
2. выполнен реальный живой прогон AI-пайплайна через AITunnel;
3. выполнен сквозной тест 3 клинических сценариев до `Диагноз` -> `Лечение` -> `Назначения`;
4. подтверждено, что код собирается и TypeScript остаётся зелёным.

Главный вывод за день: приложение стало аккуратнее и безопаснее в деталях, а основной блокер live AI-сценария сейчас находится уже не в коде, а на стороне провайдера AITunnel и бюджета ключа.

---

### Что изменено сегодня

#### 1. Безопасный просмотр документов вместо утечки в консоль

На странице документов был убран рискованный сценарий, при котором текст документа выводился через `console.log`.

Сделано:

- добавлен безопасный inline-preview документа в самом UI;
- для текстовых документов используется модальное окно с содержимым;
- для изображений используется отдельный просмотр внутри приложения;
- содержимое документов больше не утекает в DevTools по нажатию кнопки просмотра.

Основной файл:

- `src/pages/DocumentsPage.tsx`

#### 2. Дочистка typing в Gemini-адаптере

В `gemini.ts` были убраны хрупкие участки с `as any`, которые мешали дальнейшей поддержке и делали код менее предсказуемым.

Сделано:

- добавлены локальные типы для payload и результата ответа SDK;
- выделены helper-методы для сборки запроса и извлечения текста ответа;
- уменьшена зависимость от неявных структур SDK-ответа;
- код стал чище и безопаснее для будущих изменений.

Основной файл:

- `src/lib/providers/adapters/gemini.ts`

#### 3. Практический фикс таймаута для AITunnel `gpt-5.5-pro`

Во время живого прогона выяснилось, что запросы к AITunnel реально уходят, но медленная модель `gpt-5.5-pro` упирается в клиентский таймаут.

Сделано:

- таймаут в OpenAI-compatible адаптере сделан profile-aware;
- для `aitunnel + gpt-5.5-pro` увеличен лимит ожидания с 60 секунд до 180 секунд;
- текст ошибки таймаута теперь сообщает фактический лимит ожидания.

Это была точечная operational-правка, не меняющая общую архитектуру.

Основной файл:

- `src/lib/providers/adapters/openai.ts`

#### 4. Реальный live-прогон AITunnel

Сегодня был выполнен не просто compile/build-check, а реальный runtime-прогон через живой AI-профиль:

- создан и сохранён профиль `AITunnel GPT 5.5 Pro`;
- подтверждено, что приложение подхватывает профиль из хранилища;
- подтвержден реальный вызов `POST https://api.aitunnel.ru/v1/chat/completions`;
- подтвержден переход через runtime-слой приложения именно к активному профилю.

Практический результат:

- код до провайдера доходит корректно;
- основной live-блокер сейчас внешний: AITunnel вернул `402`, потому что бюджет ключа был `0 руб.`, а прогноз стоимости запроса оказался выше.

То есть проблема на этом этапе уже не в маршрутизации, не в UI и не в приложении как таковом.

#### 5. Сквозной тест 3 сценариев пациента

После live-проверки был выполнен полный сквозной прогон трёх клинических сценариев:

- пациент с подозрением на рак лёгкого;
- пациент с подозрением на рак молочной железы;
- пациент с подозрением на рак желудка.

Для каждого сценария подтвержден маршрут:

- загрузка валидных данных пациента;
- отображение диагноза;
- отображение плана лечения;
- переход на страницу назначений.

Проверка выполнялась аккуратно: из-за нестабильности browser automation при ручном вводе части полей использовался временный технический same-origin сидер для загрузки тестовых данных в draft, после чего весь UI-маршрут проходился уже штатно. После завершения теста этот вспомогательный файл был удалён и в проекте не оставлен.

#### 6. Никаких новых функциональных вырезаний

Отдельно важно: сегодня не было удаления пользовательских возможностей или "упрощений" за счёт вырезания поведения.

Подход оставался тем же:

- минимальные точечные правки;
- сохранение текущего UX и всех ключевых фич;
- исправление только того, что повышает стабильность, безопасность и пригодность к дальнейшей разработке.

---

### Что подтверждено тестами сегодня

#### Технические проверки

- `npx tsc --noEmit` проходит успешно;
- `npm run build` проходит успешно;
- диагностика по изменённым файлам после правок оставалась чистой.

#### Практические проверки

- профиль AITunnel создаётся и активируется;
- реальный запрос к внешнему AI-провайдеру инициируется корректно;
- после фикса таймаута приложение не падает мгновенно на длинном ответе;
- страницы `Диагноз`, `Лечение`, `Назначения` корректно отрабатывают на 3 сценариях пациента;
- временный тестовый инструмент после проверки удалён.

---

### Что оказалось важным по итогам дня

#### 1. Главный блокер live AI сейчас внешний

Фактическая ошибка живого сценария:

- `API Error 402`
- причина: `Превышен бюджет ключа`
- следствие: полный live AI-cycle до назначения нельзя считать завершённым именно из-за ограничений ключа, а не из-за ошибки приложения.

#### 2. Browser automation и UI формы

Во время тестирования проявилась нестабильность automation-слоя: ввод в соседние поля формы пациента мог попадать обратно в первое текстовое поле.

Это не выглядит как доказанный дефект самой формы приложения, потому что:

- маршрут через загруженные данные и дальнейший UI работал корректно;
- поведение проявлялось именно в automation-инструменте;
- после обхода проблема не влияла на реальную навигацию и отображение сценариев.

#### 3. Runtime-путь приложения подтверждён

Даже несмотря на внешний `402`, сегодня подтверждено, что:

- профиль провайдера сохраняется;
- активный профиль выбирается корректно;
- запрос реально уходит наружу;
- адаптер и маршрутизация работают по ожидаемому пути.

---

### Хронология дня

1. Завершены точечные правки после аудита: безопасность просмотра документов, дочистка typing в Gemini.
2. Выполнен живой runtime-тест через AITunnel с переданными пользователем данными.
3. Выявлен runtime bottleneck по таймауту, внесён точечный фикс в OpenAI-compatible адаптер.
4. После повторного live-прогона выявлен внешний блокер `402` по бюджету ключа.
5. Выполнен полный UI smoke/e2e по 3 клиническим сценариям до страницы назначений.
6. Временный e2e helper удалён.
7. Финально подтверждены `tsc --noEmit` и `npm run build`.

---

### Практический вывод на 2026-05-13

Проект за сегодня стал заметно аккуратнее в инженерных деталях:

- меньше хрупких typed-участков;
- чище и безопаснее работа с документами;
- лучше operational-поведение для медленного провайдера;
- подтверждён рабочий маршрут UI до конечных медицинских экранов;
- зафиксирован внешний live-блокер, который теперь легко отделяется от проблем приложения.

Если продолжать дальше, самый логичный следующий шаг:

1. получить рабочий AITunnel ключ/бюджет или другую доступную модель;
2. повторить именно live AI-прогон всех 3 пациентов без mock/mock-like обходов;
3. при успешном ответе зафиксировать уже реальные сгенерированные диагнозы, лечение и назначения от модели.

---

## Статус проекта на 2026-05-07

## Кратко

За последние 2 дня основная работа была сосредоточена на двух больших направлениях:

1. стабилизация AI-пайплайна, особенно для OpenRouter и нестабильных/free-моделей;
2. улучшение UX вокруг AI-запросов, чтобы врач мог видеть сырой ответ модели, понимать ошибки и не блокироваться во время ожидания ответа.

Текущее состояние проекта: приложение в рабочем состоянии, ключевые AI-сценарии заметно устойчивее, UI не заблокирован во время долгих запросов, а отладка ответов моделей стала значительно прозрачнее.

---

## Что сделано

### 1. OpenRouter: capability-aware логика вместо грубых эвристик

Реализовано распознавание моделей OpenRouter через metadata API OpenRouter:

- добавлен реестр моделей OpenRouter и кэш метаданных;
- профили OpenRouter автоматически обогащаются metadata при создании/обновлении;
- выбор формата структурированного ответа теперь зависит не только от имени модели, а от capability metadata;
- `response_format`, лимиты токенов и часть логики OpenAI/OpenRouter-адаптера теперь опираются на metadata модели;
- снижена зависимость от хрупких эвристик вроде простого поиска `:free` в имени модели.

Основные файлы:

- `src/lib/providers/openrouterModelRegistry.ts`
- `src/lib/providers/profileService.ts`
- `src/lib/providers/types.ts`
- `src/lib/providers/enhancedAIService.ts`
- `src/lib/providers/selector.ts`
- `src/lib/providers/adapters/openai.ts`
- `src/lib/aiServiceCompat.ts`

### 2. XML/JSON обработка для нестабильных моделей

Сделаны существенные улучшения парсинга и repair/fallback-логики:

- XML-парсер стал принимать:
  - теги с атрибутами;
  - `CDATA`;
  - self-closing теги;
  - экранированный XML;
  - альтернативные имена полей для treatment-ответов;
- если модель не вернула корректный XML, добавлен мягкий fallback;
- стало ясно, что часть моделей возвращает свободный reasoning-текст вместо XML/JSON, поэтому парсер больше не считается единственным решением, а используется вместе с UX-обходными сценариями.

### 3. Просмотр сырого ответа модели

Добавлена возможность увидеть первичный ответ нейронки до парсинга:

- кнопка `Сырой ответ ИИ` на страницах `Диагноз` и `Лечение`;
- отдельный диалог для просмотра raw response;
- в результат теперь сохраняются:
  - `rawResponse`
  - `rawResponseFormat`
  - `rawProvider`
  - `rawResponseTime`
  - `wasRepaired`

Это сильно улучшило диагностику model-specific проблем.

Основные файлы:

- `src/components/RawAiResponseDialog.tsx`
- `src/pages/DiagnosisPage.tsx`
- `src/pages/TreatmentPage.tsx`
- `src/lib/aiServiceCompat.ts`

### 4. Новый UX для ошибок AI

Ошибки AI теперь не сводятся к бесполезному `alert`.

Сделано:

- появился управляемый сценарий ошибки через плавающую AI-панель;
- при ошибке пользователь получает:
  - текст ошибки;
  - предложение перейти на более надёжную модель;
  - кнопку открытия настроек;
  - кнопку просмотра сырого ответа;
  - кнопку использования сырого ответа в следующем ручном запросе.

### 5. Повторный ручной запрос с использованием сырого ответа

Если предыдущая модель ответила плохо, но дала полезный медицинский текст, пользователь может:

- сменить модель вручную;
- включить использование сырого ответа как дополнительного контекста;
- снова отправить запрос уже в более надёжную модель.

Для этого:

- в AI prompt добавляется специальный retry-context блок;
- сырой ответ прошлой модели подмешивается только вручную и только по решению пользователя;
- это не ломает исходный сценарий и не включает скрытую автоматическую магию.

### 6. Неблокирующее окно отправки AI-запроса

Это один из самых важных UX-апдейтов.

Теперь окно предпросмотра AI-запроса:

- не является жёсткой fullscreen-модалкой;
- не блокирует приложение во время ожидания;
- перемещается мышью;
- сворачивается;
- может висеть поверх приложения как рабочее плавающее окно;
- не останавливает фоновый AI-запрос при сворачивании.

Фоновое выполнение AI-запроса вынесено в общий контекст консультации, а сверху добавлена плавающая панель статуса AI-задачи.

Основные файлы:

- `src/components/PromptPreviewDialog.tsx`
- `src/components/AiRequestPanel.tsx`
- `src/context/ConsultationContext.tsx`
- `src/components/Layout.tsx`
- `src/pages/DiagnosisPage.tsx`
- `src/pages/TreatmentPage.tsx`

### 7. Automatic Fallback

Была начата и в коде в основном реализована логика более управляемого fallback:

- одна основная модель;
- выбранные пользователем резервные модели;
- остальные профили отдельно;
- автопереход не по всем подряд профилям, а по заданной цепочке.

Это направление требует отдельного живого smoke-test, но базовые изменения уже внесены в типы, настройки и runtime-слой.

Основные файлы:

- `src/lib/providers/types.ts`
- `src/lib/providers/enhancedAIService.ts`
- `src/components/SettingsDialog.tsx`

---

## Что было исправлено по ходу

### AI / OpenRouter

- `401 Missing Authentication header`
- нестабильное поведение OpenRouter free-моделей
- `429 Provider returned error`
- `Empty AI response`
- ошибки JSON-парсинга
- ошибки XML-парсинга
- несоответствие preview prompt и фактически отправляемого prompt
- зависимость формата ответа от неподходящих эвристик

### Импорт и профили AI

- улучшен парсинг `AI providers.txt` и смежных форматов;
- исправлены проблемы, когда импортированные профили нельзя было открыть/редактировать по отдельности;
- исправлены проблемы с profile IDs и выбором карточек;
- исправлен баг `Unknown provider type: undefined` при тесте подключения.

### UI / UX

- сохранены и восстановлены корректные кнопки пола;
- выровнена вертикальная левая навигация;
- упорядочена верхняя панель действий;
- добавлен просмотр сырого AI-ответа;
- реализовано неблокирующее окно предпросмотра AI-запроса.

---

## Что протестировано

### Подтверждено

- TypeScript-диагностика по изменённым файлам чистая;
- `npx tsc -b` проходит успешно;
- ранее в этой сессии полная сборка `npm run build` проходила успешно после capability-aware и XML-фиксов;
- после последних UX-правок окно предпросмотра было доведено до рабочего состояния и подтверждено вручную.

### Что проверено вручную

- отображение сырого ответа;
- повторный AI-запрос после model-specific ошибки;
- сворачивание окна предпросмотра;
- работа AI-запроса в фоне без блокировки всего приложения;
- возможность продолжать пользоваться интерфейсом во время ожидания ответа.

---

## Остаточные риски

### 1. Нестабильные free-модели OpenRouter

Даже после улучшений часть free-моделей может:

- игнорировать формат;
- писать reasoning вместо структуры;
- возвращать неполный медицинский ответ;
- вести себя по-разному на диагнозе и лечении.

Это не полностью проблема приложения, а частично свойство конкретных моделей.

### 2. Automatic Fallback

Логика уже продвинута, но её стоит отдельно проверить в живом сценарии:

- основная модель;
- 1-2 резервные модели;
- последовательное падение первой и переход ко второй.

### 3. Полная production-сборка после последних UX-правок

TypeScript build сейчас зелёный.

Полный `vite build` на одном из последних прогонов упирался в долгую фазу `transforming...` внутри инструмента проверки, без явной compile-ошибки. Это выглядит скорее как ограничение инструмента/окружения, но при следующем рабочем заходе стоит ещё раз отдельно прогнать полный `npm run build`.

---

## Рекомендуемые следующие шаги

### Приоритет 1

Проверить и дочистить `Automatic Fallback` в живом сценарии:

- основная модель;
- резервная модель;
- ещё одна резервная модель;
- понятный порядок перехода.

### Приоритет 2

При желании усилить AI-архитектуру для слабых моделей:

- оставить генерацию как есть;
- при тяжёлых model-specific сбоях использовать более надёжную модель для ручного повторного запроса через уже добавленный raw-response retry-context.

### Приоритет 3

Вернуться к отдельной задаче, которую пользователь уже обозначал ранее:

- хранение AI-настроек/ключей в проекте в более защищённом и удобном виде;
- дальнейшая пригодность для HTML/portable-версии.

---

## Практический вывод

Проект сейчас в заметно лучшем состоянии, чем был до последних двух дней работ:

- OpenRouter стал лучше понимать, какие модели и как использовать;
- диагностика AI-ошибок стала прозрачной;
- пользователь больше не “слеп” при сбое модели;
- ожидание AI-ответа больше не парализует весь интерфейс;
- нестабильные модели по-прежнему остаются слабым звеном, но теперь у приложения есть рабочие способы это обходить без полной остановки процесса.

---

## Быстрый контекст для следующего продолжения

Если продолжать позже, логичнее всего стартовать отсюда:

1. открыть настройки AI;
2. проверить сценарий `основная модель -> резервные модели`;
3. прогнать 1 ручной диагноз и 1 ручное лечение:
   - на стабильной модели;
   - на слабой/free-модели;
4. при ошибке проверить:
   - сырой ответ;
   - переключение модели;
   - повторный ручной запрос с использованием raw-response контекста.
